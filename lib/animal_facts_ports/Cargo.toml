[package]
name = "animal_facts_ports"
description = "Defines traits for repositories"
version = "1.0.0"
edition = "2021"

[lib]
name = "animal_facts_ports"
path = "src/lib.rs"

[dependencies]
animal_facts_domain = { path = "../animal_facts_domain" }
async-trait = "~0.1"
mockall = "~0.11"
